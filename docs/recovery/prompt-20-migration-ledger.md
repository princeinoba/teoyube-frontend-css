# Prompt 20 migration ledger

Added:

- provider-neutral retrieval, embedding, vector, context, evaluation, and feedback contracts;
- server-only OpenAI embedding adapter;
- deterministic public inventory and chunker;
- ignored, local, encrypted-capable SQLite vector repository;
- resumable inventory/estimate/index/verify/rollback commands;
- exact + lexical + vector + TIG hybrid ranker;
- cross-module read-only application adapter;
- dual-consent user semantic-index service;
- Prompt 16 derivative deletion propagation;
- locked quality, privacy, deletion, injection, and boundary gates.

Retained unchanged:

- canonical static runtime;
- Next preview-only status;
- canonical WEB repository and displayed text;
- deterministic TIG;
- Prompt 13 journey state machine;
- Prompt 18 tool registry and Prompt 19 strict live response path;
- every visual source, stylesheet, asset, DOM/class contract, and baseline.

Not added:

- hosted vector store or file search;
- external website/document ingestion;
- broad RAG;
- raw sensitive user embeddings;
- production vector deployment;
- runtime cutover;
- Prompt 21.

Generated `.var/retrieval` files remain ignored and untracked.

# Live AI Production Gate

Status: **IMPLEMENTATION PREPARED; PUBLIC DEFAULT OFF; ACTIVATION DEFERRED**.

The server-only provider gateway, structured response contracts, bounded inputs/outputs, deterministic fallback, source/why-this disclosure and no-store behavior are implemented. Production and Preview flags for Live AI and the external Teo Guide provider are false. Client bundles contain no provider secret.

Activation is not approved until a server-only Vercel secret is configured without disclosure, a named model/provider is explicitly approved, paid-call authorization and cost ceiling are granted, live safety/quality evaluation passes, retention terms are verified, and rollback/kill-switch rehearsal passes. No paid call was made.

# TeoyubeWorld Media Scanner Fixtures

These fixtures contain metadata cases only. They contain no real audio, image, or video payloads.

The `empty/` folder contains only an ignored `.gitkeep` so scanner tests can verify a media-empty source.

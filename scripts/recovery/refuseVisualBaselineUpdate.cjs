"use strict";

console.error("Visual baseline updates are disabled in ordinary tooling and CI.");
console.error("A current-task owner approval ID and a separately approved workflow are required.");
process.exit(2);
